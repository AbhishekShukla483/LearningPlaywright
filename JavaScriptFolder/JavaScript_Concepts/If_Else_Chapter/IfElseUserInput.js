let num = prompt("Enter a number:");
num = Number(num);

if (num %2 === 0)
{
    console.log(num + "is an even number");
}
else
{
    console.log(num + "is a odd number");
}

//Note: --> This program needs to run in console because this program is a user input program.